#include <iostream>
#include <cmath>
using namespace std;
// en dessous: n déclaré en dehors du while (dans les accolades) = marchepas
/**
int main () {
  do {
    cout << "Entrez un nombre : ";
    double n(0.0);
    cin >> n;
  } while (n > 0.0);

  return n;
}
  **/  
  int main () {
  for(int i(0);i<5;i++){
	  int tr(2.);
	  tr++;
	  true=false
  }
  return 0;
 }
